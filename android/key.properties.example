# Copy this file to android/key.properties and keep the real values private.
# Every published update must use the same keystore and alias.
storeFile=/absolute/path/to/mobilelm-release.jks
storePassword=change-me
keyAlias=mobilelm
keyPassword=change-me
